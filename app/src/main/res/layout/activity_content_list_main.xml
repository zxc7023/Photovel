<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        layout="@layout/activity_content_list_app_bar_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:font="normal"/>

    <android.support.design.widget.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        android:background="@color/bgSlideMenuBlack"
        app:headerLayout="@layout/nav_header_main_login"
        app:menu="@menu/activity_main_drawer_login"
        app:itemBackground="@color/bgSlideMenuBlack"
        app:itemTextColor="@color/bgWhite"
        app:itemIconTint="@color/bgWhite"
        app:font="normal"/>

</android.support.v4.widget.DrawerLayout>
